package Home_work_2.test;

import Home_work_2.model.*;

public class TrainingGround {
    public static void main(String[] args) {
/*
        Warrior warrior = new Warrior("Воин");
        Zombie zombie1 = new Zombie(100);
        while (warrior.isAlive() && zombie1.isAlive()) {
            warrior.attackEnemy(zombie1);
            zombie1.attackHero(warrior);
        }
*/
/*
        Mage mage = new Mage("Маг");
        Zombie zombie = new Zombie(100);
        while (mage.isAlive() && zombie.isAlive()){
            mage.attackEnemy(zombie);
            if (mage.spell(zombie)){
                continue;
            }
            zombie.attackHero(mage);
        }
*/
/*
        Archer archer = new Archer("Лучник");
        Skeleton skeleton = new Skeleton(300);
        while (archer.isAlive() && skeleton.isAlive()){
            archer.attackEnemy(skeleton);
            skeleton.attackHero(archer);
            archer.PoisonEffect();
        }

*/

    }
}
